const path = require("path");

module.exports = {
    "config": path.resolve(__dirname, "src", "Database", "config", "index.js"),
    "migrations-path": path.resolve(__dirname, "src", "Database", "migrations"),
};
